services:
  monolith:
    build:
      context: ./backend/monolith
      dockerfile: Dockerfile
    container_name: airbnb-monolith
    env_file:
      - .env
    environment:
      PORT: 8080
    ports:
      - "${API_GATEWAY_PORT:-8080}:8080"
    restart: unless-stopped
